require "test_helper"

class EmployeesControllerTest < ActionDispatch::IntegrationTest
  # setup do
  #   Current.tenant = tenants(:one)
  #   @user = employees(:one)
  # end

  # test "should get index" do
  #   get employees_url
  #   assert_response :success
  # end

  # test "should get new" do
  #   get new_employee_url
  #   assert_response :success
  # end

  # test "should create employee" do
  #   assert_difference("User.count") do
  #     post employees_url, params: { user: { access_token: @user.access_token, tenant_id: @user.tenant_id, payroll_user_ident: @user.payroll_user_ident, last_punched_at: @user.last_punched_at, name: @user.name, pincode: @user.pincode, state: @user.state, team_id: @user.team_id } }
  #   end

  #   assert_redirected_to employees_url
  # end

  # test "should show employee" do
  #   get employee_url(@user)
  #   assert_response :success
  # end

  # test "should get edit" do
  #   get edit_employee_url(@user)
  #   assert_response :success
  # end

  # test "should update employee" do
  #   patch employee_url(@user), params: { user: { access_token: @user.access_token, tenant_id: @user.tenant_id, payroll_user_ident: @user.payroll_user_ident, last_punched_at: @user.last_punched_at, name: @user.name, pincode: @user.pincode, state: @user.state, team_id: @user.team_id } }
  #   assert_redirected_to employees_url
  # end

  # test "should destroy employee" do
  #   assert_difference("User.count", -1) do
  #     delete employee_url(@user)
  #   end

  #   assert_redirected_to employees_url
  # end
end

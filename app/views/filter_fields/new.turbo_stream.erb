<%= 
    turbo_stream.replace "li_#{@model.to_s.underscore}_#{@field}" do
      render FieldComponent.new model: @model, field: @field, filter: @filter, value: @value, selected: @selected, editable: true
    end
%>

<li class="flex items-center justify-between gap-x-6 py-5" id="<%= dom_id(punch) %>">
    <div class="min-w-0 w-full">
      <form action="" class="">    
        <input class="w-1/2 mort-form-text" type="datetime-local" id="punch_punched_at" name="punch[punched_at]" value="<%= punch.punched_at.strftime("%Y-%m-%dT%H:%M") %>" />
        <button class="mort-btn-primary">Ret</button>
      </form>
    </div>
    <div class="flex flex-none items-center gap-x-4 justify-end">
    </div>
</li>

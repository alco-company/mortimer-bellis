
<%= I18n.t("user_mailer.salutations", name: @name) %>

<%= t("user_mailer.invitation_instructions.introduction", url: root_url, sender: @resource.invited_by.name, tenant: @resource.tenant.name) %>

<%= I18n.t("user_mailer.invitation_instructions.action") %>
<%= @accept_url %>

<%== I18n.t("user_mailer.invitation_instructions.accept_until", due_date: (@resource.invitation_expires_at.to_s(:long) rescue 2.days.since)) %>

<%== I18n.t("user_mailer.invitation_instructions.ignore") %>

<%= render MortimerMailSignature.new format: :text %>

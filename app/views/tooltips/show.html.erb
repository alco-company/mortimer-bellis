<turbo-frame id="<%= params.fetch :turbo_frame, dom_id(@punch) %>" target="_top">
  <div class="relative">
    <div class="flex gap-2 items-center p-1 bg-black rounded-md text-white">
      <%#= render partial: "users/user", object: @punch, formats: :svg %>
      <strong><%= I18n.t("punch.tooltips.comment.label") %></strong>
      <%= @punch.comment %>
    </div>
    <div class="h-2 w-2 bg-black rotate-45 -top-1 -left-2 ml-[50%] relative"></div>
  </div>
</turbo-frame>